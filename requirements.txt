# Core dependencies (used by Fortran wrapper, profile loaders, batch runner)
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
pyarrow>=14.0.0

# Visualization (used by tools/, notebooks, and the legacy Streamlit demos)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0
kaleido>=0.2.1

# Utilities
Pillow>=10.0.0
requests>=2.31.0
pre-commit>=3.7.0
fpdf2>=2.7.9
openpyxl>=3.1.0

# Legacy Streamlit UI (retained for legacy/streamlit/ compatibility)
streamlit>=1.28.0

# ML extension layer (cgem_ext) — see pyproject.toml for the canonical extras.
# These are pinned here for `pip install -r requirements.txt` flows; the
# pyproject.toml extras are preferred for new development.
scikit-learn>=1.4.0
xgboost>=2.0.0
optuna>=3.5.0
shap>=0.44.0
SALib>=1.5.0
mlflow>=2.10.0

# FastAPI service (cgem_ext.api)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
structlog>=24.0.0
prometheus-client>=0.20.0

# Test + lint tooling
pytest>=8.0.0
pytest-cov>=4.1.0
ruff>=0.3.0
mypy>=1.8.0

# Optional HRV integration dependencies (see Docs/Manual.md §HRV).
# bleak>=0.21.0       # Bluetooth LE for Polar H10 connection
# neurokit2>=0.2.0    # HRV analysis algorithms
# torch>=2.0.0        # LSTM models for temporal HRV patterns

# Note: this file is the union of "Streamlit-legacy" + "cgem_ext". When the
# legacy Streamlit dashboards require `streamlit`, `kaleido`, `plotly`,
# `seaborn`, and `fpdf2`.
