These browser-test fixtures are real `/simulate-flight` responses captured on 2026-09-07 using the original CGEM binary. Flight frames are thinned to 0.5-second intervals to keep the fixtures small; all native physiological samples remain. Provenance hashes identify the original complete simulation, not the thinned fixture. Production never imports these files. Backend tests verify full-resolution dynamics and physiology; these fixtures isolate asynchronous UI behavior and deterministic browser rendering from server availability.
