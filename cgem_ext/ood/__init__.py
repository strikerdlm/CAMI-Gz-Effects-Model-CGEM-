"""Out-of-distribution detection for CGEM inputs.

Phase 2 deliverables: a Mahalanobis-distance detector with split-conformal
abstention, exposed as ``cgem_ext.ood.is_in_envelope(features) -> bool``
plus a continuous score for downstream UQ. Stub package only at Phase 0.
"""
