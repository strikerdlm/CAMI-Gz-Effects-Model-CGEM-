"""Global sensitivity analysis driven by the trained surrogate.

Phase 4 deliverables: SALib Sobol indices and Morris elementary effects
computed against the emulator (cheap because the emulator is ~10^5x
faster than the Fortran subprocess). Stub package only at Phase 0.
"""
