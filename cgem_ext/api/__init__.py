"""FastAPI service exposing the ground-truth CGEM run + the ML extension.

Phase 5 deliverables: ``main.py`` (FastAPI app), ``schemas.py`` (Pydantic
v2 models that mirror the v2.2.0 ``CGEMRun`` JSON contract pulse-sim
already consumes), and a ``Dockerfile``. Stub package only at Phase 0.
"""
